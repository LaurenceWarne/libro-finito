schema {
  query: Queries
  mutation: Mutations
}

type Book {
  title: String!
  authors: [String!]!
  description: String!
  isbn: String!
  thumbnailUri: String!
}

type Collection {
  id: String!
  name: String!
  books: [Book!]!
}

type Queries {
  books(titleKeywords: String, authorKeywords: String, maxResults: Int = 10): [Book!]!
  book(isbn: String!): Book!
  collections: [Collection!]!
  collection(id: ID!): Collection!
}

type Mutations {
  createCollection(name: String!, books: [Book!]): Collection!
  deleteCollection(id: String!): Collection!
  changeCollectionName(id: String!, name: String!): Collection!
  addBook(id: String!, book: Book!): Collection!
}
