schema {
  query: Queries
  mutation: Mutations
}

type Book {
  title: String!
  authors: [String!]!
  description: String!
  isbn: String!
  thumbnailUri: String!
}

type Collection {
  name: String!
  books: [Book!]!
}

type Queries {
  books(titleKeywords: String, authorKeywords: String, maxResults: Int = 10): [Book!]!
  book(isbn: String!): Book!
  collections: [Collection!]!
  collection(id: String!): Collection!
}

type Mutations {
  createCollection(name: String!, books: [Book!]): Collection!
  deleteCollection(id: String!): Collection!
  changeCollectionName(currentName: String!, newName: String!): Collection!
  addBook(id: String!, book: Book!): Collection!
}
